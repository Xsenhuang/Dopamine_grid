<!--pages/tasks/tasks.wxml-->
<view class="container">
  <!-- 布局分配 -->
  <view class="card layout-card">
    <text class="card-title">布局分配</text>
    <view class="layout-options">
      <view 
        wx:for="{{gridOptions}}" 
        wx:key="*this"
        class="layout-option {{gridSize === item ? 'selected' : ''}}"
        bindtap="selectGridSize"
        data-size="{{item}}"
      >
        <text>{{item}}×{{item}}</text>
      </view>
    </view>
  </view>

  <!-- 已选数量提示 -->
  <view class="selected-hint">
    <text>已选 {{selectedTaskIds.length}} 个（Bingo 使用前 {{gridSize * gridSize}} 个）</text>
  </view>

  <!-- 任务分组列表 -->
  <view class="task-groups">
    <!-- 默认分组 -->
    <view class="task-group">
      <view class="group-header">
        <view class="group-title">
          <text>默认分组</text>
        </view>
        <view class="group-select-all {{defaultGroupAllSelected ? 'selected' : ''}}" catchtap="selectAllInGroup" data-group="default">
          <text>{{defaultGroupAllSelected ? '取消' : '全选'}}</text>
        </view>
      </view>
      <view class="group-tasks">
        <view 
          wx:for="{{defaultTasks}}" 
          wx:for-index="idx"
          wx:key="id"
          class="task-tag {{selectedTaskIds.indexOf(item.id) !== -1 ? 'selected' : ''}}"
          style="{{selectedTaskIds.indexOf(item.id) !== -1 ? 'background-color: ' + (item.color || '#64B5F6') + '; border-color: ' + (item.color || '#64B5F6') + ';' : ''}}"
          bindtap="toggleTaskSelection"
          data-id="{{item.id}}"
        >
          <text>{{item.name}}</text>
        </view>
      </view>
      <!-- 添加任务输入区 -->
      <view class="add-task-section">
        <input 
          class="task-input" 
          placeholder="添加任务…" 
          value="{{newTaskName}}"
          bindinput="onInputChange"
          maxlength="20"
        />
        <view class="add-btn" bindtap="addTask">
          <text>+</text>
        </view>
      </view>
    </view>

    <!-- 其他分组 -->
    <view wx:for="{{groupedTasks}}" wx:key="groupId" wx:for-item="group" wx:if="{{group.groupId !== 'default'}}" class="task-group">
      <view class="group-header">
        <view class="group-title">
          <text>{{group.groupName}}</text>
        </view>
        <view class="group-actions">
          <view class="group-action-btn edit" catchtap="editGroup" data-group-id="{{group.groupId}}" data-group-name="{{group.groupName}}">
            <text>编辑</text>
          </view>
          <view class="group-action-btn delete" catchtap="deleteGroup" data-group-id="{{group.groupId}}">
            <text>删除</text>
          </view>
          <view class="group-select-all {{groupAllSelected[group.groupId] ? 'selected' : ''}}" catchtap="selectAllInGroup" data-group="{{group.groupId}}">
            <text>{{groupAllSelected[group.groupId] ? '取消' : '全选'}}</text>
          </view>
        </view>
      </view>
      <view class="group-tasks">
        <view 
          wx:for="{{group.tasks}}" 
          wx:for-item="task"
          wx:for-index="taskIdx"
          wx:key="id"
          class="task-tag {{selectedTaskIds.indexOf(task.id) !== -1 ? 'selected' : ''}}"
          style="{{selectedTaskIds.indexOf(task.id) !== -1 ? 'background-color: ' + (task.color || '#64B5F6') + '; border-color: ' + (task.color || '#64B5F6') + ';' : ''}}"
          bindtap="toggleTaskSelection"
          data-id="{{task.id}}"
        >
          <text>{{task.name}}</text>
        </view>
      </view>
      <!-- 添加任务输入区 -->
      <view class="add-task-section">
        <input 
          class="task-input" 
          placeholder="添加任务…" 
          value="{{newTaskNames[group.groupId] || ''}}"
          bindinput="onGroupInputChange"
          data-group-id="{{group.groupId}}"
          maxlength="20"
        />
        <view class="add-btn" bindtap="addTaskToGroup" data-group-id="{{group.groupId}}" data-group-name="{{group.groupName}}">
          <text>+</text>
        </view>
      </view>
    </view>
  </view>

  <!-- 新建分组按钮 -->
  <view class="new-group-btn" bindtap="createNewGroup">
    <text>+ 新建分组</text>
  </view>

  <!-- 底部占位，为底部按钮留出空间 -->
  <view class="bottom-spacer"></view>
</view>

<!-- 底部操作按钮区 -->
<view class="action-bar">
  <view class="action-btn {{selectedTaskIds.length === tasks.length && tasks.length > 0 ? 'active' : ''}}" bindtap="selectAll">
    <text>全选</text>
  </view>
  <view class="action-btn primary" bindtap="applySelection">
    <text>应用 ✓</text>
  </view>
  <view class="action-btn random" bindtap="randomSelect">
    <text>🎲 随机</text>
  </view>
</view>
